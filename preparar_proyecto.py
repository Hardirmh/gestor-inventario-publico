import os, zipfile; NOMBRE_ZIP = 'mi_proyecto_limpio.zip'; IGNORE_DIRS = {'venv', '.git', '__pycache__', '.idea', '.vscode'}; IGNORE_EXTS = {'.pyc', '.DS_Store'}; EXCLUDE_FILES = {'preparar_proyecto.py', 'database.db', 'mi_base_de_datos.db', NOMBRE_ZIP}; def limpiar_y_empaquetar():\n    with zipfile.ZipFile(NOMBRE_ZIP, 'w', zipfile.ZIP_DEFLATED) as zipf:\n        for root, dirs, files in os.walk('.'):\n            dirs[:] = [d for d in dirs if d not in IGNORE_DIRS]\n            for file in files:\n                if file in EXCLUDE_FILES or any(file.endswith(ext) for ext in IGNORE_EXTS): continue\n                file_path = os.path.join(root, file); zipf.write(file_path, arcname=file_path)\n    print('✅ ZIP Limpio Generado')\nif __name__ == '__main__': limpiar_y_empaquetar()
